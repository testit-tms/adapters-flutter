#!/usr/bin/env dart

import 'package:flutter_test/flutter_test.dart';

void main() {
  group('attachment', () {
    test('toAttachmentPutModel', () {
      // final client = AttachmentResponseModel();
      // expect(Counter().value, 0);
    });
  });
}
